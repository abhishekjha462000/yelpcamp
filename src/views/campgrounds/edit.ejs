<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campground</title>
    <link rel="stylesheet" href="/campgrounds/css/edit.css">
</head>

<body>
    <div class="container">
        <header>
            <h1>Edit Campground</h1>
        </header>
        <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST" class="edit-form">
            <!-- Campground Title -->
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" name="campground[title]" id="title" value="<%= campground.title %>"
                    placeholder="Enter the title" required>
            </div>

            <!-- Campground Location -->
            <div class="form-group">
                <label for="location">Location</label>
                <input type="text" name="campground[location]" id="location" value="<%= campground.location %>"
                    placeholder="Enter the location" required>
            </div>

            <!-- Buttons -->
            <div class="btn-group">
                <button type="submit" class="btn-submit">Save Changes</button>
                <a href="/campgrounds" class="btn">Back to All Campgrounds</a>
                <a href="/campgrounds/<%= campground._id %>" class="btn btn-secondary">Back to Campground</a>
            </div>
        </form>
    </div>
</body>

</html>